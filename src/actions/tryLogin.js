const { CALL_API } = require('../redux-middlewares/api')
const { LOGIN_REQUEST, LOGIN_SUCCESS, LOGIN_FAILURE } = require('../constants/action-types')
const { apiConfig } = require('../util/config-loader')

apiConfig.web_api.route.

const tryLogin = (username, password) => (dispatch, getState) => {
  return fetch()
}